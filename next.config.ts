import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // explicit PPR declaration experimental goes here
  // experimental: {
  //   ppr: 'incremental',
  // },
};

export default nextConfig;
