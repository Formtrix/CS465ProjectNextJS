import { Inter, Quicksand, Readex_Pro } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const quicksand = Quicksand({
  weight: ['400', '600', '700'],
  subsets: ['latin'],
});

export const readex = Readex_Pro({
  weight: ['400', '600', '700'],
  subsets: ['latin'],
});